@model ItemIndexViewModel
@{
    ViewData["Title"] = "Inventory";
}

<style>
    :root {
        --app-accent: #2F6F5E;
        --app-surface: #F4F8F6;
        --app-text-primary: #111827;
        --app-text-secondary: #4A7D6E;
        --app-error: #DC2626;
        --app-border-radius: 6px;
    }

    body > .container {
        max-width: none;
        width: 100%;
        padding-left: 16px;
        padding-right: 16px;
    }

    .inventory-container {
        padding: 20px;
        background-color: #FFFFFF;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        width: 100%;
        max-width: 1700px;
        margin: 0 auto;
        box-sizing: border-box;
    }

    .top-bar {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 20px;
        gap: 15px;
        flex-wrap: wrap;
    }

    .inventory-filter-form {
        display: none;
    }

    .left-controls {
        display: flex;
        gap: 10px;
    }

    .right-controls {
        display: flex;
        gap: 10px;
        align-items: center;
    }

    .nav-tabs-center {
        display: flex;
        justify-content: center;
        gap: 10px;
        margin-bottom: 20px;
        flex-wrap: wrap;
    }

    .btn {
        border-radius: var(--app-border-radius);
        font-weight: 500;
        transition: all 0.3s ease;
        border: none;
        padding: 8px 16px;
        font-size: 14px;
        cursor: pointer;
    }

    .btn-primary {
        background-color: var(--app-accent);
        color: white;
    }

    .btn-primary:hover {
        background-color: #2a5d50;
    }

    .btn-secondary {
        background-color: #6B7280;
        color: white;
    }

    .btn-secondary:hover {
        background-color: #4B5563;
    }

    .btn-info {
        background-color: #0891b2;
        color: white;
    }

    .btn-info:hover {
        background-color: #0e7490;
    }

    .btn-outline-primary {
        border: 1px solid var(--app-accent);
        color: var(--app-accent);
        background-color: transparent;
    }

    .btn-outline-primary:hover {
        background-color: var(--app-accent);
        color: white;
    }

    .btn-outline-danger {
        border: 1px solid var(--app-error);
        color: var(--app-error);
        background-color: transparent;
    }

    .btn-outline-danger:hover {
        background-color: #fee2e2;
    }

    .btn-outline-secondary {
        border: 1px solid #6B7280;
        color: #6B7280;
        background-color: transparent;
    }

    .btn-outline-secondary:hover {
        background-color: #f3f4f6;
    }

    .search-container {
        margin-bottom: 20px;
        text-align: center;
    }

    .search-label {
        display: block;
        margin-bottom: 8px;
        color: var(--app-text-primary);
        font-weight: 500;
    }

    .search-input {
        border: 1px solid var(--app-accent);
        border-radius: var(--app-border-radius);
        padding: 8px 12px;
        font-size: 14px;
        width: 100%;
        max-width: none;
        box-sizing: border-box;
    }

    .toggle-switch {
        display: flex;
        align-items: center;
        gap: 10px;
    }

    .toggle-switch input[type="checkbox"] {
        width: 40px;
        height: 24px;
        cursor: pointer;
        appearance: none;
        background-color: #ddd;
        border-radius: 12px;
        outline: none;
        position: relative;
        transition: background-color 0.3s;
    }

    .toggle-switch input[type="checkbox"]:checked {
        background-color: var(--app-accent);
    }

    .toggle-switch input[type="checkbox"]:before {
        content: '';
        position: absolute;
        width: 20px;
        height: 20px;
        border-radius: 50%;
        background-color: white;
        top: 2px;
        left: 2px;
        transition: left 0.3s;
    }

    .toggle-switch input[type="checkbox"]:checked:before {
        left: 18px;
    }

    .toggle-switch label {
        color: var(--app-text-primary);
        font-weight: 500;
        cursor: pointer;
        margin: 0;
    }

    .table-wrapper {
        border: 1px solid var(--app-accent);
        border-radius: var(--app-border-radius);
        overflow: hidden;
        flex: 1;
        display: flex;
        flex-direction: column;
        margin-bottom: 20px;
        width: 100%;
        max-width: none;
        margin-left: auto;
        margin-right: auto;
        align-self: stretch;
    }

    .table-scroll-container {
        max-height: 500px;
        overflow-y: auto;
        overflow-x: auto;
        width: 100%;
    }

    .table {
        margin: 0;
        border-collapse: collapse;
        width: 100%;
        min-width: 1420px;
        table-layout: fixed;
    }

    .table thead {
        background-color: var(--app-surface);
        border-bottom: 2px solid var(--app-accent);
        position: sticky;
        top: 0;
        z-index: 10;
    }

    .table thead th {
        color: var(--app-text-primary);
        font-weight: 600;
        padding: 12px;
        border: none;
        text-align: left;
        white-space: nowrap;
    }

    .table tbody {
        display: table-row-group;
    }

    .table tbody tr {
        border-bottom: 1px solid var(--app-accent);
        cursor: pointer;
        transition: background-color 0.2s;
    }

    .table tbody tr:hover {
        background-color: var(--app-surface);
    }

    .table tbody tr.selected {
        background-color: #e8f5e9;
    }

    .table tbody td {
        padding: 12px;
        color: var(--app-text-primary);
        vertical-align: middle;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
        max-width: 200px;
    }

    .bottom-actions {
        display: flex;
        justify-content: center;
        gap: 20px;
        padding: 20px;
        border-top: 1px solid var(--app-surface);
    }

    .modal-overlay {
        display: none;
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background-color: rgba(0, 0, 0, 0.5);
        z-index: 1000;
        align-items: center;
        justify-content: center;
    }

    .modal-overlay.show {
        display: flex;
    }

    .modal-content {
        background-color: white;
        border-radius: var(--app-border-radius);
        padding: 20px;
        max-width: 600px;
        width: 90%;
        max-height: 90vh;
        overflow-y: auto;
        box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
    }

    .modal-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 20px;
        border-bottom: 1px solid var(--app-surface);
        padding-bottom: 10px;
    }

    .modal-title {
        font-size: 18px;
        font-weight: 600;
        color: var(--app-text-primary);
    }

    .modal-close {
        background: none;
        border: none;
        font-size: 24px;
        cursor: pointer;
        color: #999;
    }

    .form-group {
        margin-bottom: 15px;
    }

    .form-group label {
        display: block;
        margin-bottom: 5px;
        color: var(--app-text-primary);
        font-weight: 500;
        font-size: 14px;
    }

    .form-group input,
    .form-group textarea {
        width: 100%;
        padding: 8px 12px;
        border: 1px solid var(--app-accent);
        border-radius: var(--app-border-radius);
        font-size: 14px;
        font-family: inherit;
        box-sizing: border-box;
    }

    .form-group textarea {
        min-height: 80px;
        resize: vertical;
    }

    .form-group input:focus,
    .form-group textarea:focus {
        outline: none;
        border-color: var(--app-accent);
        box-shadow: 0 0 0 2px rgba(47, 111, 94, 0.1);
    }

    .form-row {
        display: flex;
        gap: 15px;
        margin-bottom: 15px;
    }

    .form-row .form-group {
        flex: 1;
        margin-bottom: 0;
    }

    .modal-buttons {
        display: flex;
        justify-content: flex-end;
        gap: 10px;
        margin-top: 20px;
        border-top: 1px solid var(--app-surface);
        padding-top: 15px;
    }

    .error-message {
        color: var(--app-error);
        font-size: 13px;
        margin-top: 5px;
    }

    .no-items-message {
        text-align: center;
        padding: 40px 20px;
        color: #9CA3AF;
        font-size: 16px;
    }

    .get-data-button {
        margin-bottom: 15px;
        width: 100%;
    }

    .form-section-title {
        font-size: 14px;
        font-weight: 600;
        color: var(--app-text-primary);
        margin-top: 15px;
        margin-bottom: 10px;
    }

    .scrollbar-container::-webkit-scrollbar {
        width: 8px;
        height: 8px;
    }

    .scrollbar-container::-webkit-scrollbar-track {
        background: var(--app-surface);
    }

    .scrollbar-container::-webkit-scrollbar-thumb {
        background: var(--app-accent);
        border-radius: 4px;
    }

    .scrollbar-container::-webkit-scrollbar-thumb:hover {
        background: #2a5d50;
    }
</style>

<div class="inventory-container">
    <form id="inventoryFilterForm" asp-controller="Items" asp-action="Index" method="get" class="inventory-filter-form"></form>

    <!-- Top Bar with Statistics and Toggle -->
    <div class="top-bar">
        <div class="left-controls">
            <a asp-controller="Statistics" asp-action="Index" class="btn btn-info">Statistics</a>
        </div>
        <div class="right-controls">
            <span style="color: var(--app-text-primary); font-weight: 500;">Show expired items</span>
            <div class="toggle-switch">
                <input type="checkbox" id="showExpiredToggle" name="showExpiredOnly" value="true" form="inventoryFilterForm" @(Model.ShowExpiredOnly ? "checked" : string.Empty) onchange="submitInventoryFilters()">
            </div>
        </div>
    </div>

    <!-- Center Navigation Buttons -->
    <div class="nav-tabs-center">
        <button class="btn btn-primary">Items</button>
        <a asp-controller="Substances" asp-action="Index" class="btn btn-secondary">Substances</a>
        <a asp-controller="Orders" asp-action="Manage" class="btn btn-secondary">Orders</a>
    </div>

    <!-- Search Bar -->
    <div class="search-container">
        <label class="search-label">Search by name</label>
        <input type="text" id="searchInput" name="searchQuery" value="@Model.SearchQuery" form="inventoryFilterForm" class="search-input" placeholder="Enter name..." oninput="filterItems()">
    </div>

    <!-- Table -->
    @if (!Model.Items.Any())
    {
        <div class="no-items-message">
            <p>No items found.</p>
        </div>
    }
    else
    {
        <div class="table-wrapper">
            <div class="table-scroll-container scrollbar-container">
                <table class="table" id="itemsTable">
                    <thead>
                        <tr>
                            <th style="width: 50px;">ID</th>
                            <th style="width: 170px;">Name</th>
                            <th style="width: 170px;">Producer</th>
                            <th style="width: 90px;">Price</th>
                            <th style="width: 150px;">Category</th>
                            <th style="width: 360px;">Description</th>
                            <th style="width: 80px;">Pills</th>
                            <th style="width: 90px;">Quantity</th>
                            <th style="width: 170px;">Label</th>
                            <th style="width: 90px;">Discount</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (Item item in Model.Items)
                        {
                            <tr class="data-row" data-id="@item.Id" data-name="@item.Name" onclick="selectRow(this)">
                                <td>@item.Id</td>
                                <td>@item.Name</td>
                                <td>@item.Producer</td>
                                <td>@item.Price.ToString("F2")</td>
                                <td>@item.Category</td>
                                <td>@item.Description</td>
                                <td>@item.NumberOfPills</td>
                                <td>@item.Quantity</td>
                                <td>@item.Label</td>
                                <td>@item.DiscountPercentage</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    }

    <!-- Bottom Action Buttons -->
    <div class="bottom-actions">
        <button class="btn btn-primary" onclick="openAddModal()">Add</button>
        <button class="btn btn-primary" onclick="openUpdateModal()">Update</button>
        <button class="btn btn-outline-danger" onclick="removeItem()">Remove</button>
    </div>
</div>

<!-- Add Item Modal -->
<div id="addModal" class="modal-overlay">
    <div class="modal-content">
        <div class="modal-header">
            <h2 class="modal-title">Add Item</h2>
            <button class="modal-close" onclick="closeAddModal()">&times;</button>
        </div>

        <form id="addItemForm">
            <div class="form-group">
                <label>Name *</label>
                <input type="text" id="addName" name="Name" required>
            </div>

            <div class="form-group">
                <label>Producer *</label>
                <input type="text" id="addProducer" name="Producer" required>
            </div>

            <div class="form-group">
                <label>Price *</label>
                <input type="number" id="addPrice" name="Price" step="0.01" required>
            </div>

            <div class="form-group">
                <label>Category *</label>
                <input type="text" id="addCategory" name="Category" required>
            </div>

            <div class="form-row">
                <div class="form-group">
                    <label>Number of Pills</label>
                    <input type="number" id="addNumberOfPills" name="NumberOfPills">
                </div>

                <div class="form-group">
                    <label>Quantity</label>
                    <input type="number" id="addQuantity" name="Quantity">
                </div>
            </div>

            <div class="form-group">
                <label>Label</label>
                <input type="text" id="addLabel" name="Label">
            </div>

            <div class="form-group">
                <label>Description</label>
                <textarea id="addDescription" name="Description"></textarea>
            </div>

            <div class="form-group">
                <label>Discount (%)</label>
                <input type="number" id="addDiscount" name="DiscountPercentage" step="0.01">
            </div>

            <div class="modal-buttons">
                <button type="button" class="btn btn-outline-secondary" onclick="closeAddModal()">Cancel</button>
                <button type="submit" class="btn btn-primary">Add Item</button>
            </div>
        </form>
    </div>
</div>

<!-- Update Item Modal -->
<div id="updateModal" class="modal-overlay">
    <div class="modal-content">
        <div class="modal-header">
            <h2 class="modal-title">Update Item</h2>
            <button class="modal-close" onclick="closeUpdateModal()">&times;</button>
        </div>

        <form id="updateItemForm">
            <div class="form-row">
                <div class="form-group" style="flex: 0.5;">
                    <label>ID *</label>
                    <input type="number" id="updateId" name="Id" required>
                </div>
                <div style="flex: 0.5;"></div>
            </div>

            <button type="button" class="btn btn-primary get-data-button" onclick="getItemData()">Get Item Data</button>

            <div class="form-group">
                <label>Name *</label>
                <input type="text" id="updateName" name="Name" required>
            </div>

            <div class="form-group">
                <label>Producer *</label>
                <input type="text" id="updateProducer" name="Producer" required>
            </div>

            <div class="form-group">
                <label>Price *</label>
                <input type="number" id="updatePrice" name="Price" step="0.01" required>
            </div>

            <div class="form-group">
                <label>Category *</label>
                <input type="text" id="updateCategory" name="Category" required>
            </div>

            <div class="form-row">
                <div class="form-group">
                    <label>Number of Pills</label>
                    <input type="number" id="updateNumberOfPills" name="NumberOfPills">
                </div>

                <div class="form-group">
                    <label>Quantity</label>
                    <input type="number" id="updateQuantity" name="Quantity">
                </div>
            </div>

            <div class="form-group">
                <label>Label</label>
                <input type="text" id="updateLabel" name="Label">
            </div>

            <div class="form-group">
                <label>Description</label>
                <textarea id="updateDescription" name="Description"></textarea>
            </div>

            <div class="form-group">
                <label>Discount (%)</label>
                <input type="number" id="updateDiscount" name="DiscountPercentage" step="0.01">
            </div>

            <div class="modal-buttons">
                <button type="button" class="btn btn-outline-secondary" onclick="closeUpdateModal()">Cancel</button>
                <button type="submit" class="btn btn-primary">Update Item</button>
            </div>
        </form>
    </div>
</div>

<script>
    let selectedRow = null;
    const allItems = @Html.Raw(Newtonsoft.Json.JsonConvert.SerializeObject(Model.Items, new Newtonsoft.Json.JsonSerializerSettings { NullValueHandling = Newtonsoft.Json.NullValueHandling.Ignore }));

    function selectRow(row) {
        if (selectedRow) {
            selectedRow.classList.remove('selected');
        }
        row.classList.add('selected');
        selectedRow = row;
    }

    function openAddModal() {
        document.getElementById('addModal').classList.add('show');
        document.getElementById('addItemForm').reset();
    }

    function closeAddModal() {
        document.getElementById('addModal').classList.remove('show');
    }

    function openUpdateModal() {
        document.getElementById('updateModal').classList.add('show');
        document.getElementById('updateId').focus();
    }

    function closeUpdateModal() {
        document.getElementById('updateModal').classList.remove('show');
    }

    function submitInventoryFilters() {
        document.getElementById('inventoryFilterForm').submit();
    }

    function getItemData() {
        const itemId = document.getElementById('updateId').value;
        if (!itemId) {
            alert('Please enter an item ID first!');
            return;
        }

        const item = allItems.find(i => i.Id == itemId);
        if (item) {
            document.getElementById('updateName').value = item.Name || '';
            document.getElementById('updateProducer').value = item.Producer || '';
            document.getElementById('updatePrice').value = item.Price || '';
            document.getElementById('updateCategory').value = item.Category || '';
            document.getElementById('updateNumberOfPills').value = item.NumberOfPills || '';
            document.getElementById('updateQuantity').value = item.Quantity || '';
            document.getElementById('updateLabel').value = item.Label || '';
            document.getElementById('updateDescription').value = item.Description || '';
            document.getElementById('updateDiscount').value = item.DiscountPercentage || '';
        } else {
            alert('Item not found!');
            console.log('Available items:', allItems);
            console.log('Searching for ID:', itemId);
        }
    }

    function filterItems() {
        const searchValue = document.getElementById('searchInput').value.toLowerCase();
        const rows = document.querySelectorAll('.data-row');

        rows.forEach(row => {
            const name = row.dataset.name.toLowerCase();
            const matchesSearch = name.includes(searchValue);
            row.style.display = matchesSearch ? '' : 'none';
        });
    }

    function removeItem() {
        if (!selectedRow) {
            alert('Please select an item first!');
            return;
        }

        if (confirm('Are you sure you want to delete this item?')) {
            const itemId = selectedRow.dataset.id;
            window.location.href = `/Items/Delete?id=${itemId}`;
        }
    }

    document.getElementById('addItemForm').addEventListener('submit', async function(e) {
        e.preventDefault();
        const formData = new FormData(this);
        try {
            const response = await fetch('/Items/Create', {
                method: 'POST',
                body: formData
            });
            if (response.ok) {
                location.reload();
            }
        } catch (error) {
            alert('Error adding item: ' + error);
        }
    });

    document.getElementById('updateItemForm').addEventListener('submit', async function(e) {
        e.preventDefault();
        const formData = new FormData(this);
        const itemId = document.getElementById('updateId').value;
        try {
            const response = await fetch(`/Items/Edit?id=${itemId}`, {
                method: 'POST',
                body: formData
            });
            if (response.ok) {
                location.reload();
            }
        } catch (error) {
            alert('Error updating item: ' + error);
        }
    });
</script>
